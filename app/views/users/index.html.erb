<h1>Users :</h1>


<% @users.each do |user| %>
    <h2>first_name : <%= user.first_name %></h2>
    <p>last_name : <%= user.last_name %><p>
    <p>
    <a href="<%= user_path(user.id) %>" class="btn btn-primary">Show more</a>
    </p>
<% end %>